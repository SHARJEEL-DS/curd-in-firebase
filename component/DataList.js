import { useCollectionData } from "react-firebase-hooks/firestore";
import { database } from "../firebase";

const DataList = () => {
  // const dataRef = database.collection("Name"); // Replace with your Firestore collection name
// 
  // const [data, loading, error] = useCollectionData(dataRef, { idField: "3BplipjljlUeFcqrhFTR" });

  // if (loading) {
  //   return <p>Loading...</p>;
  // }

  // if (error) {
  //   return <p>Error: {error.message}</p>;
  // }

  return (
    <div>
      tt
    </div>
  );
};

export default DataList;
